Ext.define( 'extAdmin.component.feature.ActionsLauncher',
{
	/**
	 * Initializes actions
	 * 
	 * @param {Object} config
	 */
	initActions : function( config )
	{
		for( var actionType in config ) {
			if( config.hasOwnProperty( actionType ) === false ) {
				continue;
			}
			
			extAdmin.
		}
	}
});